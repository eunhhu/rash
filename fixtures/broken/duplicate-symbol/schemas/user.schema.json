{
  "$schema": "https://rash.dev/schemas/schema.json",
  "name": "User",
  "description": "User schema (first definition)",

  "definitions": {
    "User": {
      "type": "object",
      "properties": {
        "id": { "type": "string", "format": "uuid" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" }
      }
    }
  }
}
